import type { DesktopPathInfo } from './contracts';

export async function getDesktopPathInfo(): Promise<DesktopPathInfo | null> {
  // Step 2 placeholder.
  // Step 3+ can wire this to Tauri invoke contracts.
  return null;
}
