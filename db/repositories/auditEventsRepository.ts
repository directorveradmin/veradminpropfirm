export class AuditEventsRepository {
    insertAuditEvent(audit: any): string {
        // TODO: insert audit event
        return '';
    }

    getAuditHistory(accountId: string, limit = 50) {
        // TODO: fetch audit events for account
        return [];
    }
}
