export class AccountTimelineRepository {
    getTimelineEvents(accountId: string, limit = 100) {
        // TODO: merge trade logs, notes, payouts, mode changes, restrictions, alerts
        return [];
    }
}
